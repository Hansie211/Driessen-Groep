<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="App.RegisterPage">
    <ContentPage.Content>
        <StackLayout>

            <Label Text="Voornaam" />
            <Entry Placeholder="Voornaam" Text="{Binding FirstName}" />
            
            <Label Text="Acternaam" />
            <Entry Placeholder="Acternaam" Text="{Binding LastName}" />

            <Label Text="Email adres"/>
            <Entry Placeholder="Email adres" Keyboard="Email"  Text="{Binding EmailAddress}"/>

            <Label Text="Wachtwoord"/>
            <Entry Placeholder="Wachtwoord" IsPassword="true" Text="{Binding Password}" />
            <Label Text="Bevestig wachtwoord"/>
            <Entry Placeholder="Bevestig wachtwoord" IsPassword="true" Text="{Binding PasswordConfirm}"/>

            <Label Text="{Binding Error}" HorizontalOptions="CenterAndExpand" FontAttributes="Bold" TextColor="Red" HeightRequest="30"/>

            <Button Text="Account aanmaken" Command="{Binding RegisterCommand}" HorizontalOptions="FillAndExpand" />

        </StackLayout>
    </ContentPage.Content>
</ContentPage>