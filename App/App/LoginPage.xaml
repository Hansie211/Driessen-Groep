<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="App.LoginPage">
    <ContentPage.Content>
        <StackLayout>

            <Label Text="Email adres"/>
            <Entry Placeholder="Email adres" Keyboard="Email" x:Name="edtEmailAddress"/>

            <Label Text="Wachtwoord" />
            <Entry Placeholder="Wachtwoord" IsPassword="true" x:Name="edtPassword" />

            <Label x:Name="lblError" HorizontalOptions="CenterAndExpand" FontAttributes="Bold" TextColor="Red" HeightRequest="30"/>
            
            <Button Text="Login" Clicked="ButtonLogin_Clicked" HorizontalOptions="FillAndExpand"/>

            <Label Text="Nog geen account? Registreer hier" HorizontalOptions="Center" Margin="0,15,0,0">
                <Label.GestureRecognizers>
                    <TapGestureRecognizer Tapped="LabelRegister_Clicked" />
                </Label.GestureRecognizers>
            </Label>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>